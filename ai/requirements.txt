fastapi==0.115.0
uvicorn[standard]==0.30.6
# ultralytics==8.3.0 membutuhkan torch+CUDA yang sangat besar (~2GB).
# Untuk CPU-only (laptop tanpa GPU), install torch dulu dari PyPI CPU index.
# Dockerfile menginstall ini secara terpisah untuk mengontrol versi.
ultralytics==8.3.0
anthropic==0.34.0
python-multipart==0.0.12
boto3==1.35.0
Pillow==10.4.0
python-dotenv==1.0.1
httpx==0.27.2
